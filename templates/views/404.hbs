<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="css/styles.css"/>
</head>
<body>
  {{>header}}
  <h2>{{title}}</h2>
  <p>{{description}} </p>
  {{>footer}}
</body>
</html>